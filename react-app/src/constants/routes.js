export const routes = {
  maps: {
    path: ({ mode }) => `/${mode}/maps`,
  },
  mappers: {
    path: ({ mode }) => `/${mode}/mappers`,
  },
  farmers: {
    path: ({ mode }) => `/${mode}/farmers`,
  },
};
